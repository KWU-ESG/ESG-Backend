spring:
  datasource:
    url: jdbc:mysql://esg-project.cfbqz6prbj9w.ap-northeast-2.rds.amazonaws.com:3306/example?serverTimezone=UTC&characterEncoding=UTF-8
    username: esg_admin
    password: esgproject
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: esg.xce6de.ng.0001.apn2.cache.amazonaws.com
    port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
